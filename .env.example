# Copiar a ".env" para desarrollo local del módulo de Balance (npm run dev:server).
# En Railway, DATABASE_URL la inyecta automáticamente el plugin de Postgres
# una vez lo agregas al proyecto desde el dashboard — no hace falta configurarla ahí.

DATABASE_URL=postgres://usuario:password@localhost:5432/tabla_movimientos
PORT=4173
